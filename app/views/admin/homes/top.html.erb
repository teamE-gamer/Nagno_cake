注文履歴一覧
<table>
  <tr>
    <td>購入日時</td>
    <td>購入者</td>
    <td>注文個数</td>
    <td>注文ステータス</td>
  </tr>
    <% @orders.each do |order| %>
      <tr>
          <td>
              <%= link_to admin_order_path(id: order.id) do %>
              <%=  order.created_at %>
            <% end %>
          </td>
          <td><%= order.name %></td>
          <td><%= order.order_details.sum(:amount) %></td>
          <td><%= order.order_status %></td>
      </tr>
    <% end %>
</table>